---
phase: 01-derived-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/013_add_quota_metrics.sql
autonomous: true

must_haves:
  truths:
    - "Querying quota_metrics returns remaining_pct for every LLP/species/year combination"
    - "Querying quota_metrics returns risk_level (critical/warning/ok/na) matching percentage thresholds"
    - "Zero-allocation rows return NULL for remaining_pct and 'na' for risk_level (no database errors)"
    - "SQL COMMENT on quota_remaining and quota_metrics views documents formulas and assumptions"
  artifacts:
    - path: "sql/migrations/013_add_quota_metrics.sql"
      provides: "Migration creating quota_metrics view and adding COMMENTs"
      contains: "CREATE OR REPLACE VIEW quota_metrics"
    - path: "sql/migrations/013_add_quota_metrics.sql"
      provides: "Documentation comments"
      contains: "COMMENT ON VIEW"
  key_links:
    - from: "quota_metrics view"
      to: "quota_remaining view"
      via: "SELECT * FROM quota_remaining"
      pattern: "FROM quota_remaining"
    - from: "risk_level values"
      to: "app/utils/formatting.py RISK_COLORS"
      via: "matching keys"
      pattern: "critical|warning|ok|na"
---

<objective>
Create the `quota_metrics` SQL view with derived percentage and risk level columns, and add documentation comments to both `quota_remaining` and `quota_metrics` views.

Purpose: Move derived metric calculations from Python to SQL, ensuring consistent calculations across all queries and enabling SQL-level filtering/sorting on these metrics.

Output:
- Migration file `013_add_quota_metrics.sql`
- `quota_metrics` view with `remaining_pct` and `risk_level` columns
- SQL COMMENTs documenting both views
</objective>

<execution_context>
@C:\Users\vikra\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vikra\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-derived-metrics/01-RESEARCH.md

# Source files for reference
@sql/schema-v2-multi-tenant.sql (lines 252-284 for quota_remaining view)
@app/utils/formatting.py (risk level logic and RISK_COLORS keys)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quota_metrics migration</name>
  <files>sql/migrations/013_add_quota_metrics.sql</files>
  <action>
Create migration file with:

1. **quota_metrics view** that extends quota_remaining with:
   - `remaining_pct`: `ROUND((remaining_lbs / NULLIF(allocation_lbs, 0) * 100)::NUMERIC, 2)`
   - `risk_level`: CASE expression with conditions ordered correctly:
     - `allocation_lbs = 0` -> 'na' (check FIRST to avoid division)
     - `remaining_pct < 10` -> 'critical'
     - `remaining_pct < 50` -> 'warning'
     - ELSE -> 'ok' (NOT 'healthy' - must match Python RISK_COLORS keys)
   - Include `WITH (security_invoker = true)` for RLS compatibility (Postgres 15+)

2. **COMMENT ON VIEW quota_remaining** documenting:
   - Formula: remaining = allocation + transfers_in - transfers_out - harvested
   - Assumptions: is_deleted filtering, all amounts in lbs, harvests from eLandings only

3. **COMMENT ON VIEW quota_metrics** documenting:
   - Formulas for remaining_pct and risk_level with thresholds
   - Zero-allocation handling (NULL for pct, 'na' for risk)
   - RLS note about security_invoker

Pattern to follow from existing migration `012_bycatch_hauls.sql` lines 181-186 for COMMENT syntax.

IMPORTANT: Use 'ok' not 'healthy' for risk_level >= 50% - this matches existing Python code in `app/utils/formatting.py` line 62.
  </action>
  <verify>
    - File exists: `ls sql/migrations/013_add_quota_metrics.sql`
    - Contains NULLIF: `grep -c "NULLIF" sql/migrations/013_add_quota_metrics.sql` returns >= 1
    - Contains security_invoker: `grep -c "security_invoker" sql/migrations/013_add_quota_metrics.sql` returns >= 1
    - Contains all risk levels: `grep -E "critical|warning|ok|na" sql/migrations/013_add_quota_metrics.sql`
    - Contains COMMENTs: `grep -c "COMMENT ON VIEW" sql/migrations/013_add_quota_metrics.sql` returns >= 2
  </verify>
  <done>
    Migration file exists with quota_metrics view definition, safe division using NULLIF, risk_level CASE expression with 'ok' (not 'healthy'), and COMMENT ON VIEW for both views.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate migration SQL syntax</name>
  <files>sql/migrations/013_add_quota_metrics.sql</files>
  <action>
Validate the migration SQL is syntactically correct by:

1. Check for common SQL syntax issues:
   - Balanced parentheses
   - Proper CASE/END matching
   - Semicolons after each statement
   - No trailing commas before FROM

2. Review risk_level CASE ordering:
   - allocation_lbs = 0 MUST be first (before division)
   - Critical (< 10) MUST come before warning (< 50)
   - Confirm ELSE returns 'ok'

3. Verify column list completeness:
   - All columns from quota_remaining are included (qr.*)
   - New columns: remaining_pct, risk_level

4. If any issues found, fix them in the migration file.

Note: We cannot run the migration against the database from Claude (requires Supabase dashboard or CLI with credentials). This task validates syntax only.
  </action>
  <verify>
    - Re-read migration file and confirm:
      - All SQL statements end with semicolon
      - CASE has matching END
      - View definition has balanced parentheses
      - Risk level order is: na (alloc=0), critical (<10), warning (<50), ok (else)
  </verify>
  <done>
    Migration SQL is syntactically valid, CASE conditions are correctly ordered (most specific first), and all required columns are present.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Migration file exists at `sql/migrations/013_add_quota_metrics.sql`
2. File contains:
   - `CREATE OR REPLACE VIEW quota_metrics` with `security_invoker = true`
   - `remaining_pct` calculation using NULLIF for safe division
   - `risk_level` CASE with 'na', 'critical', 'warning', 'ok' values
   - `COMMENT ON VIEW quota_remaining` with formula documentation
   - `COMMENT ON VIEW quota_metrics` with metrics documentation
3. SQL syntax validated (no obvious syntax errors)
4. Risk level values match Python RISK_COLORS keys exactly
</verification>

<success_criteria>
- [ ] METR-01: quota_metrics view includes remaining_pct calculated from remaining_lbs/allocation_lbs
- [ ] METR-02: quota_metrics view includes risk_level with correct thresholds (critical <10%, warning <50%, ok >=50%)
- [ ] METR-03: Division by zero handled via NULLIF (returns NULL, not error)
- [ ] METR-04: SQL COMMENT documents both quota_remaining and quota_metrics views
- [ ] Risk level uses 'ok' not 'healthy' to match existing Python code
- [ ] View uses security_invoker=true for RLS compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/01-derived-metrics/01-01-SUMMARY.md` using the summary template.
</output>
